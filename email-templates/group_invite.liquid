<!DOCTYPE html>
<html lang="{{ lang | default: 'en' }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ subject }}</title>
    <style>
        :root {
            --primary: #5271ff;
            --primary-dark: #3d5eff;
            --text: #333333;
            --text-light: #6b7280;
            --bg: #ffffff;
            --border: #e5e7eb;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            line-height: 1.6;
            color: var(--text);
            background-color: #f9fafb;
            padding: 20px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: var(--bg);
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            overflow: hidden;
        }

        .header {
            background-color: var(--primary);
            padding: 30px 40px;
            text-align: center;
            color: white;
        }

        .header h1 {
            font-size: 24px;
            font-weight: 600;
            margin: 0;
        }

        .content {
            padding: 40px;
        }

        p {
            margin-bottom: 16px;
            color: var(--text);
        }

        .button-container {
            text-align: center;
            margin: 30px 0;
        }

        .button {
            display: inline-block;
            background-color: var(--primary);
            color: white;
            text-decoration: none;
            padding: 12px 28px;
            border-radius: 8px;
            font-weight: 500;
            font-size: 16px;
            transition: background-color 0.2s;
        }

        .button:hover {
            background-color: var(--primary-dark);
        }

        .link-fallback {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: var(--text-light);
        }

        .link-fallback a {
            color: var(--primary);
            text-decoration: none;
            word-break: break-all;
        }

        .footer {
            background-color: #f9fafb;
            padding: 20px;
            text-align: center;
            font-size: 14px;
            color: var(--text-light);
            border-top: 1px solid var(--border);
        }

        @media screen and (max-width: 480px) {
            .header, .content {
                padding: 20px;
            }
            
            .header h1 {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1>
            {% if lang == 'de-DE' %}
                Sie wurden eingeladen, "{{ group_name }}" beizutreten
            {% else %}
                You are invited to join "{{ group_name }}"
            {% endif %}
        </h1>
    </div>
    <div class="content">
        {% if lang == 'de-DE' %}
            <p>{{ inviter }} hat Sie eingeladen, der Gruppe "{{ group_name }}" als {{ role }} beizutreten.</p>
            <p>Wenn Sie noch kein Konto haben, können Sie eines erstellen, indem Sie auf die Schaltfläche unten klicken.</p>
            <div class="button-container">
                <a href="{{ host }}/groups" class="button">Ihre Einladungen anzeigen</a>
            </div>
            <div class="link-fallback">
                <p>Wenn Sie die Schaltfläche nicht sehen können, besuchen Sie bitte den folgenden Link:</p>
                <a href="{{ host }}/groups">{{ host }}/groups</a>
            </div>
        {% else %}
            <p>{{ inviter }} has invited you to join the group "{{ group_name }}" as a {{ role }}.</p>
            <p>If you do not have an account, you can create one by clicking the button below.</p>
            <div class="button-container">
                <a href="{{ host }}/groups" class="button">View your invitations</a>
            </div>
            <div class="link-fallback">
                <p>If you cannot see the button, please visit the following link:</p>
                <a href="{{ host }}/groups">{{ host }}/groups</a>
            </div>
        {% endif %}
    </div>
    <div class="footer">
        <p>
            {% if lang == 'de-DE' %}
                Vielen Dank für die Nutzung unseres Services!
            {% else %}
                Thank you for using our service!
            {% endif %}
        </p>
    </div>
</div>
</body>
</html>