<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ projectName }} Email Service</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="generator" content="Directus" />
    <meta name="x-apple-disable-message-reformatting" />
    <meta name="robots" content="noindex,follow" />

    <style type="text/css">
      * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
      }

      /* CLIENT-SPECIFIC STYLES */
      body, table, td, a {
          -webkit-text-size-adjust: 100%;
          -ms-text-size-adjust: 100%;
          -webkit-font-smoothing: antialiased;
      }
      
      table, td {
          mso-table-lspace: 0pt;
          mso-table-rspace: 0pt;
      }
      
      img {
          -ms-interpolation-mode: bicubic;
          border: 0;
          height: auto;
          line-height: 100%;
          outline: none;
          text-decoration: none;
      }
      
      table {
          border-collapse: collapse !important;
      }
      
      body {
          height: 100% !important;
          margin: 0 !important;
          padding: 0 !important;
          width: 100% !important;
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
          line-height: 1.6;
          color: #333333;
          background-color: #f9fafb;
      }

      /* iOS BLUE LINKS */
      a[x-apple-data-detectors] {
          color: inherit !important;
          text-decoration: none !important;
          font-size: inherit !important;
          font-family: inherit !important;
          font-weight: inherit !important;
          line-height: inherit !important;
      }
      
      body a {
          color: {{ projectColor }};
          text-decoration: underline;
      }
      
      p {
          margin-bottom: 16px;
          color: #333333;
      }
      
      h1 {
          font-size: 22px;
          font-weight: 600;
          color: #333333;
          margin: 0;
          padding-bottom: 20px;
      }
      
      hr {
          margin: 40px 0 20px 0;
          width: 100%;
          border-top: 1px solid #e5e7eb;
          border-bottom: none;
          border-left: none;
          border-right: none;
      }
      
      div.im, span.im {
          color: inherit !important;
      }

      .wrapper {
          padding: 20px;
      }

      .container {
          max-width: 600px;
          margin: 0 auto;
          background-color: #ffffff;
          border-radius: 12px;
          box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
          overflow: hidden;
      }

      .header {
          background-color: {{ projectColor }};
          padding: 30px 40px;
          text-align: center;
          color: white;
      }

      .content {
          padding: 40px;
      }

      .footer {
          background-color: #f9fafb;
          padding: 20px;
          text-align: center;
          font-size: 14px;
          color: #6b7280;
          border-top: 1px solid #e5e7eb;
      }

      .logo {
          max-width: 180px;
          height: auto;
          margin-bottom: 15px;
          display: block;
          margin-left: auto;
          margin-right: auto;
      }

      .small-logo {
          height: 40px;
          width: 40px;
          border: 1px solid white;
          border-radius: 8px;
          padding: 10px;
          background: {{ projectColor }};
          object-fit: contain;
      }

      .signature-logo {
          height: 22px;
          width: 22px;
          border-radius: 100%;
          border: 1px solid #ffffff;
          background: {{ projectColor }};
          padding: 11px;
          display: block;
      }

      .signature {
          display: flex;
          align-items: center;
          margin: 20px 0;
      }

      .signature-text {
          margin-left: 8px;
          line-height: 20px;
      }

      .signature-title {
          color: #737373;
      }

      .footer-text {
          margin: 10px 0;
          font-size: 13px;
          line-height: 16px;
          color: #171717;
      }

      /* MOBILE STYLES */
      @media screen and (max-width: 600px) {
          .header, .content {
              padding: 20px;
          }
          
          .header h1 {
              font-size: 18px;
          }
      }

    </style>
  </head>
  <body>

    <div class="wrapper">
      <div class="container">
        <div class="header">
          <img src="{{ projectLogo }}" alt="{{ projectName }} Logo" class="logo" />
          <h1>{% block header_title %}{{ projectName }} Email Service{% endblock %}</h1>
        </div>
        
        <div class="content">
          {% block content %}{{ html }}{% endblock %}
        </div>

        <div class="footer">
          <p class="footer-text">
            Sent by {{ projectName }}{% if projectUrl != '' %} —
            <a href="{{ projectUrl }}" target="_blank">Manage Your Account</a>{% endif %}
            <br />
            {% block footer %}{% endblock %}
          </p>
        </div>
      </div>
    </div>
  </body>
</html>